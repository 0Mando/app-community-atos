import { ShortTitleArticlePipe } from './short-title-article.pipe';

describe('ShortTitleArticlePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortTitleArticlePipe();
    expect(pipe).toBeTruthy();
  });
});
