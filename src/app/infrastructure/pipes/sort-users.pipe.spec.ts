import { SortUsersPipe } from './sort-users.pipe';

describe('SortUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new SortUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
