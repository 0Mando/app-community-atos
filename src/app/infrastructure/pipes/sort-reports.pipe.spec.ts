import { SortReportsPipe } from './sort-reports.pipe';

describe('SortReportsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortReportsPipe();
    expect(pipe).toBeTruthy();
  });
});
