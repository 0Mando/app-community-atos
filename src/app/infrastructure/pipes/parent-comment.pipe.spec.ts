import { ParentCommentPipe } from './parent-comment.pipe';

describe('ParentCommentPipe', () => {
  it('create an instance', () => {
    const pipe = new ParentCommentPipe();
    expect(pipe).toBeTruthy();
  });
});
