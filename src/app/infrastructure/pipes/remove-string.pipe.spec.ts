import { RemoveStringPipe } from './remove-string.pipe';

describe('RemoveStringPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveStringPipe();
    expect(pipe).toBeTruthy();
  });
});
