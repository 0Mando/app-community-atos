import { FormatUrlPipe } from './format-url.pipe';

describe('FormatUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
