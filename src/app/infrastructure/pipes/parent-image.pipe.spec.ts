import { ParentImagePipe } from './parent-image.pipe';

describe('ParentImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ParentImagePipe();
    expect(pipe).toBeTruthy();
  });
});
